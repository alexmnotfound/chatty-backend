DATABASE_URL="postgresql://chatty:chatty_dev@localhost:5433/chatty"
PORT=3000
FRONTEND_URL="http://localhost:5173"

# IMPORTANT: Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="cambiar-en-produccion-minimo-32-chars"

# WhatsApp Business (Meta Cloud API)
# These are fallback values. Per-company credentials are configured via the Settings page.
WHATSAPP_VERIFY_TOKEN="tu-token-de-verificacion-personalizado"
WHATSAPP_ACCESS_TOKEN=""
WHATSAPP_PHONE_NUMBER_ID=""
# App Secret from Meta Developer Console → App Settings → Basic → App Secret
WHATSAPP_APP_SECRET=""


# OpenAI (fallback — per-company keys configured via Settings page)
OPENAI_API_KEY=""
